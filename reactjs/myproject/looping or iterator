looping or iterator: is very import 
===================
definition: a set of same statements executes more than one times until condition is true.
types:
======
* for loop - fixed interation
* while loop - unfixed iteration with entry loop
* do...while loop - unfixed iteration exit loop
* nested loop - a loop with i a loopqw

for loop:
=========
    syntax:
    for(initial;condition;increment/decrement)
    {
       statement;
    }
    execution steps:
    step 1: intial (startig value)
            ex:int i=1
    step 2: condition (true/false)
            ex: i<10
            if true  means goto step 3
            if false means end loop
    step 3: statement
            ex: s.o.p(i)
    step 4: increment/decrement
            ex: i++ or i=i+1 or i+=1
    step 5: step2
    ex:
    for(int i=5;i<17;i+=3)
    {
     s.o.p(i);
    }
     o/p: 5 8 11 14
           
           
           
